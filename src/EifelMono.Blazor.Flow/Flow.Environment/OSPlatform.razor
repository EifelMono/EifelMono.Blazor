@namespace EifelMono.Blazor.Flow

<If Value="@System.Runtime.InteropServices.RuntimeInformation.IsOSPlatform(System.Runtime.InteropServices.OSPlatform.Windows)">
    @Windows
</If>
<If Value="@System.Runtime.InteropServices.RuntimeInformation.IsOSPlatform(System.Runtime.InteropServices.OSPlatform.Linux)">
    @Linux
</If>
<If Value="@System.Runtime.InteropServices.RuntimeInformation.IsOSPlatform(System.Runtime.InteropServices.OSPlatform.OSX)">
    @OSX
</If>

@code {
    [Parameter]
    public RenderFragment Windows { get; set; }
    [Parameter]
    public RenderFragment Linux { get; set; }
    [Parameter]
    public RenderFragment OSX { get; set; }
}
