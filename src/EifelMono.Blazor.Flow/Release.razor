<If Value="@IsRelease">
    @ChildContent
</If>

@code
{
    [Parameter]
    public RenderFragment ChildContent { get; set; }
    private bool IsRelease
    {
        get
        {
#if DEBUG
            return false;
#else
return true;
#endif
        }
    }
}
