@namespace EifelMono.Blazor.Flow

<If Value="@FlowGlobals.IsDebug">
    <Then>
        @Debug
    </Then>
    <Else>
        @Release
    </Else>
</If>

@code {
    [Parameter]
    public RenderFragment Debug { get; set; }

    [Parameter]
    public RenderFragment Release { get; set; }
}
